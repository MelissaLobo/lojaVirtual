CREATE TABLE usuario
(
  id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  nome VARCHAR (30)NOT NULL,
  email VARCHAR (30)NOT NULL,
  senha VARCHAR (30)NOT NULL,
PRIMARY KEY (id)
);

CREATE TABLE Produtos
(
  id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  nome VARCHAR (30)NOT NULL,
  valor INT NOT NULL,
  descricao VARCHAR (100)NOT NULL,
  marca VARCHAR (30)NOT NULL,
  categoria VARCHAR (30)NOT NULL,
  usuario_id INT,
  PRIMARY KEY (id)
);

CREATE TABLE comentarios (
  id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  produto_id INT,
  nome VARCHAR(255)NOT NULL,
  texto VARCHAR(5000)NOT NULL,
  PRIMARY KEY (id)
);

CREATE TABLE carrinho (
  id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  usuario_id INT,
  produto_id INT,
  PRIMARY KEY (id)
);

Ligar Banco de Dados:
Digite o comando dentro da pasta lib do Banco de dados:
java -cp hsqldb.jar org.hsqldb.server.Server --database.0 file:lojaVirtual --dbname.0 lojaVirtual

Parar abrir o Banco de Dados digite o comando:
java -cp hsqldb.jar  org.hsqldb.util.DatabaseManager